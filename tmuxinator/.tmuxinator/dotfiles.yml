name: dotfiles
root: ~/lab/dotfiles

on_project_start: docker compose up -d

on_project_stop: docker compose stop

windows:
  - editor:
      root: ~/lab/dotfiles
      layout: main-vertical
      panes:
        - nvim .
      # commands:
      #   - $EDITOR .
