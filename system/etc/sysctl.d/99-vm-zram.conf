# Tune kernel VM for zram swap on a desktop with ample RAM (31GiB)
# Lowers swappiness to avoid premature anonymous page swapping
# Lowers vfs_cache_pressure to keep hot file cache in memory longer
vm.swappiness = 10
vm.vfs_cache_pressure = 50
