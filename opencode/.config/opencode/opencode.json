{
  "$schema": "https://opencode.ai/config.json",
  "agent": {
    "code-reviewer": {
      "description": "Reviews code for best practices and potential issues",
      "prompt": "You are a code reviewer reviewing the current branch. Focus on security, performance, and maintainability. Check for bugs, anti-patterns, and suggest improvements.",
      "tools": {
        "write": false,
        "edit": false
      }
    },
    "explore": {
      "disable": true
    },
    "general": {
      "disable": true
    }
  },
  "command": {
    "test": {
      "template": "Run the full test suite with coverage report and show any failures.\nFocus on the failing tests and suggest fixes.",
      "description": "Run tests with coverage",
      "agent": "build"
    }
  },
  "plugin": [
    "oh-my-opencode-slim"
  ],
  "lsp": true
}
